name: Deploy

# Scheduled runs still collect current status; Pages deploys only when visible data changes.
# Actions performance metrics are collected at most once per UTC day and cached separately.

on:
  workflow_dispatch:
  schedule:
    - cron: "11 * * * *"
  push:
    branches: [main]
    paths:
      - "dashboard/**"
      - "repositories/catalog.yml"
      - ".github/workflows/deploy-dashboard.yml"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: dashboard
    outputs:
      changed: ${{ steps.changes.outputs.changed }}
    steps:
      - name: Checkout
        timeout-minutes: 2
        uses: actions/checkout@v4

      - name: Set up Python
        timeout-minutes: 2
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: dashboard/requirements.txt

      - name: Install dependencies
        timeout-minutes: 2
        run: pip install -r requirements.txt

      - name: Run tests
        timeout-minutes: 2
        run: python -m unittest discover -s tests -v

      - name: Prepare dashboard config
        timeout-minutes: 2
        run: >-
          python src/prepare_dashboard_config.py
          --config dashboard.yml
          --output .dashboard.runtime.yml

      - name: Select daily Actions metrics cache
        id: metrics-day
        shell: bash
        run: echo "date=$(date -u +%F)" >> "$GITHUB_OUTPUT"

      - name: Restore daily Actions metrics
        id: metrics-cache
        timeout-minutes: 2
        uses: actions/cache/restore@v4
        with:
          path: dashboard/site/action-metrics.json
          key: dashboard-action-metrics-${{ steps.metrics-day.outputs.date }}
          restore-keys: |
            dashboard-action-metrics-

      - name: Collect daily Actions metrics
        id: metrics-collect
        if: steps.metrics-cache.outputs.cache-hit != 'true'
        timeout-minutes: 5
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          DASHBOARD_TOKEN: ${{ secrets.DASHBOARD_TOKEN }}
        run: |
          set -euo pipefail
          python src/collect_action_metrics.py \
            --config .dashboard.runtime.yml \
            --output site/action-metrics.json \
            --changed-output .action-metrics-changed \
            --valid-output .action-metrics-valid
          echo "changed=$(cat .action-metrics-changed)" >> "$GITHUB_OUTPUT"
          echo "valid=$(cat .action-metrics-valid)" >> "$GITHUB_OUTPUT"

      - name: Save daily Actions metrics
        if: steps.metrics-cache.outputs.cache-hit != 'true' && steps.metrics-collect.outputs.valid == 'true'
        timeout-minutes: 2
        uses: actions/cache/save@v4
        with:
          path: dashboard/site/action-metrics.json
          key: dashboard-action-metrics-${{ steps.metrics-day.outputs.date }}

      - name: Generate dashboard
        timeout-minutes: 5
        env:
          GITHUB_TOKEN: ${{ github.token }}
          DASHBOARD_TOKEN: ${{ secrets.DASHBOARD_TOKEN }}
          DASHBOARD_ADMIN_TOKEN: ${{ secrets.DASHBOARD_ADMIN_TOKEN }}
        run: >-
          python src/dashboard_entry.py
          --config .dashboard.runtime.yml
          --output site/index.html
          --hash-output .dashboard-content-hash

      - name: Compare with deployed dashboard
        timeout-minutes: 2
        id: changes
        shell: bash
        env:
          PAGES_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/
          METRICS_CHANGED: ${{ steps.metrics-collect.outputs.changed }}
        run: |
          set -euo pipefail

          new_hash="$(tr -d '\r\n' < .dashboard-content-hash)"
          current_hash="$(
            curl --fail --silent --show-error --location --max-time 20 \
              "${PAGES_URL}?_check=${GITHUB_RUN_ID}" 2>/dev/null \
              | sed -n 's/.*name="dashboard-content-hash" content="\([^"]*\)".*/\1/p' \
              | head -n 1 \
              || true
          )"

          echo "Current deployed hash: ${current_hash:-<none>}"
          echo "New dashboard hash    : $new_hash"
          echo "Actions metrics changed: ${METRICS_CHANGED:-false}"

          if [[ "${METRICS_CHANGED:-false}" == "true" ]]; then
            echo "Daily Actions metrics changed; GitHub Pages will be deployed."
            echo "changed=true" >> "$GITHUB_OUTPUT"
          elif [[ -n "$current_hash" && "$current_hash" == "$new_hash" ]]; then
            echo "No dashboard-visible changes; skipping GitHub Pages deployment."
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "Dashboard-visible state changed; GitHub Pages will be deployed."
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Configure Pages
        timeout-minutes: 2
        if: steps.changes.outputs.changed == 'true'
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        timeout-minutes: 2
        if: steps.changes.outputs.changed == 'true'
        uses: actions/upload-pages-artifact@v3
        with:
          path: dashboard/site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: build
    if: needs.build.outputs.changed == 'true'
    steps:
      - name: Deploy to GitHub Pages
        timeout-minutes: 2
        id: deployment
        uses: actions/deploy-pages@v4
